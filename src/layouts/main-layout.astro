---
import BaseLayout from './base-layout.astro';
const { title, description, tags, author, cover } = Astro.props;
---

<BaseLayout
  title={title}
  description={description}
  tags={tags}
  author={author}
  cover={cover}
>
  <slot />
</BaseLayout>